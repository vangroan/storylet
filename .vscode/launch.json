{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "gdbpath": "gdb",
            "type": "gdb",
            "request": "launch",
            "target": "./target/debug/storylet",
            "cwd": "${workspaceRoot}"
        }
    ]
}